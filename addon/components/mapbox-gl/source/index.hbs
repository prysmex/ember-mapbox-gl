{{this.onUpdateArgs this.updateSource @options}}
{{yield
  (hash
    layer=(component
      'mapbox-gl/layer'
      map=@map
      _sourceId=this.sourceId
      cacheKey=@cacheKey
      cache=@cache
    )
    terrain=(component 'mapbox-gl/terrain' map=@map sourceId=this.sourceId)
    id=this.sourceId
  )
}}